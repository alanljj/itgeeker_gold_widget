# 2026-07-22 工作日志

## 任务 1
用户希望对一份"资深开发工程师赋能团队技术能力提升"的中文需求 Prompt 进行分析和增强。

### 产出
- `D:\git_geeker\itgeeker_gold_widget\需求增强-团队技术能力提升.md`
  - 含 4 部分：差异总览、增强版完整 Prompt、使用说明、增强设计要点
  - 增强要点：角色锁定、OKR 化目标、可量化指标、起步动作反问、12 周里程碑

### 关键决策
- 不直接重写需求，而是提供"原版 → 增强版"对照表，让用户看清改了哪里、为什么改
- 用方括号 `[...]` 标记需要用户填空的位置（团队规模、技术栈、痛点等）
- 强调"先反问再给方案"，避免资深工程师给出脱离实际的空报告

## 任务 2：替换运行时图标 + 修复 exe 缺失图标问题
- 用户新增了 `img/gold_widget.ico` 和 `img/gold_widget_310x310_Logo.png`，要求替换原先的 Emoji 运行时图标
- 根因：原 `tray.py` 用 `make_emoji_icon(APP_EMOJI)` 绘制，spec 文件没 `icon=` 参数 → exe 缺图标
- 修改文件：
  - `config.py` — 新增 `resource_path()` 兼容 PyInstaller `_MEIPASS`，定义 `RUNTIME_APP_ICON/LOGO`，版本升到 1.3.2.0
  - `tray.py` — 新增 `load_app_icon()`：优先 ICO → 次选 PNG → 兜底 Emoji
  - `main.py` — 用 `load_app_icon(128)` 替换 `make_emoji_icon(...)`
  - `itgeeker_gold_widget.spec` — 改用 `SPEC` 内置变量（修复了硬编码错误路径），新增 `datas` 打包 img，新增 `icon=ICON_PATH`
- ⚠️ **没有自动打包**，按用户习惯提供 `pyinstaller` 命令让用户手动测试
- ⚠️ 旧 spec 路径 `geeker_dev\python_dev\py_fin\itgeeker_gold_widget` 与当前项目路径 `git_geeker\itgeeker_gold_widget` 不一致，已修复

## 任务 3：确认并持久化 uv 环境约定（2026-07-22 09:57）
- 用户要求把以下环境约定写入记忆，后续命令全部用 uv：
  - 项目根：`D:\git_geeker\itgeeker_gold_widget`
  - venv：`.venv/`（项目内）
  - Python 3.14
  - 装包：`uv add/remove`；运行：`uv run`；禁止 `pip`
- 实际核查磁盘：
  - `.python-version` = 3.14 ✓
  - `pyproject.toml requires-python=">=3.14"` ✓
  - `.venv/Scripts/python.exe` 实测 3.14 ✓
  - ⚠️ `pyproject.toml dependencies=[]`，.venv 里**没装** PySide6/requests（之前给的测试命令走的是 sandbox 的 3.13.13，不是用户的 venv）
- 已更新 `MEMORY.md`：
  - 修正旧路径 `geeker_dev\python_dev\py_fin\widget_gold_price` → 实际 `git_geeker\itgeeker_gold_widget`
  - 新增"环境与工具链约定"段落
  - 标记"依赖现状：待补"
  - 更新运行/打包命令为 uv 风格
- **未自动 `uv add`**，等用户确认是否要现在装依赖

## 任务 4：版本升级到 v1.3.3.0 + 完整打包（2026-07-22 10:13~10:39）
- 用户明确要求"更新版本号并打包成exe文件" → 覆盖"先测后打"默认约定
- **版本号统一更新到 v1.3.3.0**（10 处）：
  - `config.py` 5+13
  - `main.py` 4
  - `main_window.py` 5
  - `tray.py` 4
  - `settings_dialog.py` 4（之前漏改的 v1.3.1.0）
  - `gold_api.py` 8（之前漏改的 v1.3.1.0）
  - `version_info.py` 19+20+35+39
  - `README.md` 3
- **uv 装依赖**：
  - `uv add PySide6 requests` → 装上 PySide6 6.11.1 + requests 2.34.2（5秒，命中缓存）
  - `uv add --dev pyinstaller` → 装上 PyInstaller 6.21.0（6秒）
- **Smoke test 通过**：图标多尺寸 16/24/32/48/64 加载正常
- **PyInstaller 跑成**：`uv run pyinstaller --clean --noconfirm itgeeker_gold_widget.spec`
  - 第一次跑后台 task `80J8nj` 跑了 22 分钟无果（系统清理了 task id），旧 exe 仍是 5月日期
  - 第二次跑前台（timeout=300s），成功完成（实际 ~10 秒分析 + ~5 秒 COLLECT）
  - 关键日志：`Copying icon to EXE` / `Copying version information to EXE` / `Build complete!`
- **产物**（`dist/`）：
  - `ITGeekerGoldWidget.exe` 5.0 MB
  - `_internal/` 117 MB（含 24 个 DLL/PYD + img/）
  - `_internal/img/gold_widget.ico` 145K + `gold_widget_310x310_Logo.png` 184K
- **PE 校验**（用 pefile 读 exe 元数据）：
  - VS_FIXEDFILEINFO FileVersion = 1.3.3.0 ✓
  - ProductVersion = 1.3.3.0 ✓
  - StringFileInfo 9 个字段全部正确（含 CompanyName、FileDescription、Copyright）
- ⚠️ 后台 task 状态丢失的教训：PyInstaller 这类 IO 密集型任务，未来跑前台（加 timeout）或用 `run_in_background` 后必须等通知，不要无脑轮询

