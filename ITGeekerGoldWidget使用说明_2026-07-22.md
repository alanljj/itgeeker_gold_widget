# 把黄金行情放到桌面角落：ITGeeker Gold Widget 开源黄金价格小组件介绍

每天关注黄金价格，很多人的习惯是打开网页、切换行情页面、刷新数据，再在多个页面之间反复查看。

如果你只想在桌面上快速了解当前金价、涨跌幅和行情来源，其实不需要打开一个庞大的行情软件。

**ITGeeker Gold Widget** 是一款由技术奇客 ITGeeker.net 开源的 Windows 桌面黄金价格小组件。它将黄金行情做成一个轻量、透明、可拖动、可置顶的桌面浮窗，让用户不用频繁打开浏览器，也能随时查看黄金价格变化。

> 项目官网：<https://www.itgeeker.net>  
> 当前版本：`v1.3.3.0`  
> 开源协议：MIT License  
> 适用平台：Windows 10 / 11

---

## 一、ITGeeker Gold Widget 是什么？

ITGeeker Gold Widget 的定位很简单：

> **把黄金价格固定在桌面上，让关注金价变得更加方便。**

启动后，软件会以一个无边框浮窗的形式显示在桌面上。窗口可以自由拖动，也可以调整大小、设置透明度、修改颜色和字体。用户还可以将它设置为始终置顶，或者让它随 Windows 开机自动启动。

小组件主要显示以下信息：

- 当前黄金价格
- 涨跌金额
- 涨跌幅
- 开盘价
- 最高价
- 最低价
- 上一次收盘价
- 更新时间
- 当前数据来源
- 当前货币和计价单位

目前支持两种行情模式：

- **CNY：人民币 / 克**
- **USD：美元 / 盎司**

国内用户可以查看沪金 Au9999 行情，关注国际黄金价格的用户则可以切换到美元 / 盎司模式。

---

## 二、主要功能介绍

### 1. 多数据源自动切换

行情类工具最担心的问题之一，就是某个数据接口暂时不可用。

ITGeeker Gold Widget 没有把所有功能依赖在单一数据源上，而是在代码中设计了多级数据源回退机制。

在人民币模式下，程序会按照优先级尝试获取：

1. 新浪财经 Au9999 行情
2. GoldPrice.org 人民币黄金价格
3. Yahoo Finance 国际金价，并通过美元人民币汇率进行换算

在美元模式下，程序会尝试：

1. Yahoo Finance `GC=F`
2. GoldPrice.org 美元黄金价格

当某个数据源访问失败时，程序会自动尝试下一个数据源。只有所有数据源都无法获取时，界面才会提示价格获取失败。

这种设计可以减少因为单个接口临时异常而导致的行情空白问题，也让小组件在日常使用时更加稳定。

需要说明的是，软件展示的是第三方公开行情数据，数据刷新情况会受到网络连接、数据源服务状态和接口限制影响，不能将其视为交易所级别的实时交易报价。

---

### 2. CNY 与 USD 一键切换

ITGeeker Gold Widget 支持国内黄金价格和国际黄金价格两种观察方式。

用户可以在窗口中的货币切换按钮上直接切换，也可以在设置窗口中选择：

- 人民币 / 克
- 美元 / 盎司

人民币模式更适合关注国内黄金零售价格、沪金 Au9999 等信息的用户。

美元模式则更适合关注国际金价、COMEX 黄金期货以及美元资产的用户。

切换后，程序会重新获取对应模式的数据，并更新价格单位、涨跌信息和数据来源。

---

### 3. 无边框透明浮窗

传统行情软件通常拥有完整的窗口边框、工具栏和大量功能，对于只想查看价格的用户来说可能显得比较复杂。

ITGeeker Gold Widget 采用无边框窗口设计，默认使用圆角、半透明背景和顶部渐变效果，整体更接近桌面小组件，而不是传统行情软件。

窗口具备以下特性：

- 无边框显示
- 半透明背景
- 圆角卡片效果
- 顶部高光渐变
- 可自由移动
- 可调整窗口大小
- 可设置窗口置顶

用户可以将它放在桌面边缘、显示器角落，或者放在常用工作区域中，减少对日常工作的干扰。

---

### 4. 外观完全可定制

软件提供了独立的设置窗口，可以根据个人桌面主题调整显示效果。

目前支持的外观配置包括：

- 背景颜色
- 背景透明度
- 文字颜色
- 字体大小

背景透明度可以在一定范围内自由调节，字体大小也可以根据显示器分辨率和个人阅读习惯进行修改。

如果你喜欢深色桌面，可以使用深色背景与浅色文字；如果希望小组件更容易识别，也可以提高不透明度和字体大小。

这些配置会保存到本地，下次启动时会自动恢复。

---

### 5. 定时刷新与手动刷新

软件启动后会立即请求一次行情数据，之后通过定时器自动刷新。

默认刷新间隔为 **60 秒**，用户可以在设置中调整刷新频率，范围为：

- 最短 10 秒
- 最长 3600 秒

除了自动刷新外，用户还可以通过窗口右键菜单或系统托盘菜单执行“立即刷新”。

在数据请求期间，界面会显示“刷新中……”等状态提示。行情请求通过后台线程执行，不会直接阻塞主界面，避免窗口在网络较慢时出现明显卡顿。

---

### 6. 系统托盘常驻

ITGeeker Gold Widget 支持系统托盘运行。

关闭或隐藏窗口后，程序仍然可以继续驻留在系统托盘中。用户可以通过以下方式重新显示窗口：

- 双击托盘图标
- 单击托盘图标
- 在托盘菜单中选择显示窗口

托盘菜单提供了常用操作：

- 显示 / 隐藏窗口
- 立即刷新
- 设置窗口置顶
- 打开设置
- 访问官网
- 查看关于信息
- 退出程序

对于需要长期运行的小工具来说，托盘常驻可以避免程序占用任务栏空间，也方便用户随时调出或隐藏。

---

### 7. 窗口位置和大小记忆

很多桌面小工具在重启后会恢复到默认位置，用户每次都需要重新调整。

ITGeeker Gold Widget 会记住窗口的：

- 屏幕位置
- 窗口宽度
- 窗口高度
- 置顶状态
- 外观设置
- 刷新间隔
- 当前货币模式

下一次启动时，窗口会尽量恢复到上次使用的位置和尺寸。

配置文件默认保存在：

```text
%USERPROFILE%\itgeeker_widget_config\config_gold.json
```

配置采用 JSON 格式保存，便于备份、查看和迁移。

---

### 8. 支持开机自动启动

如果你每天都会关注黄金行情，可以在设置窗口中勾选“开机自动启动”。

程序会将启动配置写入当前 Windows 用户的启动项中。这样系统启动后，软件就可以自动运行，避免每次手动打开。

该功能基于 Windows 注册表实现，因此目前项目的主要使用平台是 Windows。

---

## 三、使用界面与操作方式

### 拖动窗口

在窗口非交互区域按住鼠标左键，即可将小组件拖动到桌面任意位置。

### 调整大小

拖动窗口右下角的调整区域，可以改变小组件尺寸。

### 设置置顶

通过窗口右键菜单或托盘菜单，可以切换：

- 窗口置顶
- 取消置顶

置顶后，即使切换到其他应用，黄金价格小组件也会保持在其他窗口上方。

### 切换货币

点击窗口中的货币切换按钮，可以在 CNY 和 USD 模式之间切换。

### 手动刷新

右键点击窗口，选择“立即刷新”，即可手动请求最新数据。

### 退出程序

如果只是想隐藏窗口，可以关闭窗口或选择隐藏。

如果要完全退出程序，需要在窗口右键菜单或托盘菜单中选择“退出”。

---

## 四、适合哪些用户？

### 个人黄金投资者

如果你经常关注沪金 Au9999、黄金首饰价格或黄金市场波动，可以将小组件放在桌面上，随时查看行情变化。

### 关注国际黄金价格的用户

美元 / 盎司模式适合关注国际黄金价格、COMEX 黄金期货以及全球市场动态的用户。

### 桌面小工具爱好者

如果你喜欢桌面常驻工具，又不希望安装复杂的行情软件，那么这个小组件可以作为一个简单的桌面信息卡片。

### 需要自定义桌面布局的用户

软件支持自由拖动、窗口置顶、透明度调节、字体调整和颜色修改，可以融入不同的桌面主题和工作环境。

### Python 开发者

项目基于 Python 和 PySide6 开发，代码结构相对清晰，也适合作为学习桌面 GUI、系统托盘、后台线程、定时任务和 PyInstaller 打包的参考项目。

---

## 五、项目使用的技术方案

ITGeeker Gold Widget 采用以下技术实现：

- Python
- PySide6
- Qt for Python
- Requests
- QThread
- QTimer
- QSystemTrayIcon
- PyInstaller
- JSON 配置文件
- Windows 注册表启动项

核心模块大致分工如下：

| 文件 | 作用 |
| --- | --- |
| `main.py` | 程序入口和 Qt 应用初始化 |
| `main_window.py` | 主窗口、界面展示、交互和刷新逻辑 |
| `gold_api.py` | 黄金行情请求、数据源切换和价格转换 |
| `settings_dialog.py` | 设置窗口和配置项界面 |
| `config.py` | 配置读取、保存和开机启动管理 |
| `tray.py` | 系统托盘图标和托盘菜单 |
| `version_info.py` | Windows 可执行文件版本信息 |
| `itgeeker_gold_widget.spec` | PyInstaller 打包配置 |

其中，`gold_api.py` 负责处理行情数据，`main_window.py` 负责展示数据。数据请求放在后台线程中执行，界面线程负责更新控件，从而实现界面和网络请求的分离。

---

## 六、如何从源码运行？

如果你希望直接阅读源码或参与二次开发，可以按照以下方式运行。

### 1. 准备 Python 环境

项目仓库中包含 Python 项目配置。README 中写明支持 Python 3.10+，但当前 `pyproject.toml` 和 `.python-version` 使用的是 Python 3.14。

为了减少环境差异，建议使用 Python 3.14。

### 2. 安装依赖

```bash
pip install -r requirements.txt
```

主要依赖包括：

```text
PySide6
requests
```

### 3. 启动程序

```bash
python main.py
```

启动后，程序会在桌面显示黄金价格小组件，并自动请求行情数据。

---

## 七、如何打包成 Windows 程序？

项目提供了 PyInstaller 配置文件，可以将源码打包成 Windows 可执行文件。

安装 PyInstaller：

```bash
pip install pyinstaller
```

执行打包命令：

```bash
python -m PyInstaller itgeeker_gold_widget.spec --clean --noconfirm
```

打包完成后，可以在 `dist/` 目录中找到：

```text
ITGeekerGoldWidget.exe
```

项目还提供了图标和 Windows 版本信息配置，打包后的程序可以显示软件名称、版本号、公司名称和版权信息。

需要注意的是，仓库中的 `dist/` 目录属于构建产物，并不代表项目已经提供了面向最终用户的正式安装包。实际发布时，还需要根据发布渠道制作压缩包、安装程序或便携版程序。

---

## 八、开源与二次开发

ITGeeker Gold Widget 使用 MIT License 开源。

这意味着开发者可以在遵守许可证要求的前提下，对项目进行学习、修改和二次开发。

你可以基于现有项目继续扩展，例如：

- 增加白银、铂金等贵金属行情
- 增加更多数据源
- 增加历史价格走势图
- 增加价格提醒功能
- 增加系统通知
- 增加多显示器适配
- 增加更多语言界面
- 增加 Linux 或 macOS 支持
- 增加配置导入和导出
- 增加独立安装程序

项目已经具备桌面窗口、托盘、网络请求、配置持久化和打包发布等基础能力，适合作为进一步开发的起点。

---

## 九、目前的使用限制

为了准确介绍项目，也需要说明目前可以确认的限制：

1. 项目主要面向 Windows 10 / 11。
2. 程序运行时需要访问外部行情接口，因此需要网络连接。
3. 行情数据来自第三方公开数据源，数据可用性和刷新时间取决于网络及数据源状态。
4. 项目没有提供用户数量、下载量或性能基准数据。
5. 仓库中没有完整的跨平台测试矩阵。
6. 软件展示行情信息，不构成任何投资建议。
7. 黄金价格、汇率和数据源服务可能随时变化，实际显示结果应以具体数据源为准。

---

## 十、总结

ITGeeker Gold Widget 并不是一个复杂的行情交易平台，它更像是一个专注于“快速查看黄金价格”的桌面工具。

它将以下功能组合在了一起：

- 黄金价格获取
- 多数据源自动回退
- CNY / USD 双模式切换
- 无边框透明浮窗
- 窗口置顶
- 桌面拖拽和调整大小
- 系统托盘常驻
- 定时刷新
- 外观自定义
- 窗口位置记忆
- 开机自动启动
- PyInstaller Windows 打包
- MIT 协议开源

如果你只想在桌面上放一个轻量的黄金价格窗口，不想频繁打开网页，也不需要复杂的交易功能，那么 ITGeeker Gold Widget 是一个值得尝试的选择。

把它放在桌面角落，工作、学习或浏览网页时，抬眼就能看到黄金行情。

> **ITGeeker Gold Widget：让黄金价格，始终在桌面触手可及的位置。**

---

## 文章摘要

ITGeeker Gold Widget 是一款基于 Python 和 PySide6 开发的 Windows 黄金价格桌面小组件，支持 CNY 人民币 / 克与 USD 美元 / 盎司两种模式，集成新浪财经、GoldPrice.org 和 Yahoo Finance 等多个数据源，并提供自动故障转移、定时刷新、窗口置顶、透明外观、系统托盘、开机自启和配置记忆等功能。项目采用 MIT License 开源，适合黄金价格关注者、桌面工具爱好者以及 Python 开发者使用和二次开发。

## 推荐关键词

```text
ITGeeker Gold Widget
黄金价格小组件
黄金行情桌面工具
实时黄金价格
Windows 黄金软件
Python PySide6 项目
桌面浮窗工具
黄金价格监控
开源桌面小工具
CNY USD 黄金价格
```
